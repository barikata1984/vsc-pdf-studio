## 0. 土台の整合

- [x] 0.1 導入済みで動作している版にだけ入っている表示側の修正 4 ファイル分（手書き層での指スクロールの自前実装、スクロール対象の受け渡し 1 行、指描画の状態を表す種別の付け外しの除去、指の操作に関する見た目の指定）を作業ツリーへ取り込む。取り込み後、導入済み版の該当ファイルと内容が一致することを md5 で確認する
- [x] 0.2 拡張機能の設定ファイルの版番号を 0.0.8 に上げ、変更履歴に 0.0.6、0.0.7、0.0.8 の項を追加する。0.0.8 の項は本 change の内容とする
- [x] 0.3 `npm ci`、`npm test`、`npm run lint` を順に実行し、すべて成功することを確認する。結果を記録に残す
- [x] 0.4 記録用のディレクトリ `notes/debug/` を作り、`notes/debug/<日付>_render-performance-baseline.md` を作る。測定に使う文書（ページ数の少ないもの、数百ページのもの、手書きと注釈を含むもの）と測定手順を書く
- [x] 0.5 手動確認の一覧を `notes/debug/<日付>_manual-check-list.md` として作る。項目は、文書の表示、拡大縮小、スクロール、現在ページの表示、ページ番号指定の移動、目次からの移動、ページ一覧の表示と移動、文字の検索と一致箇所の強調、テキスト選択、蛍光ペンと下線と取り消し線、付箋、入力欄への記入、手書き（ペンと指の両方）、取り消し操作、保存と開き直し。各項目に期待する結果を書く
- [ ] 0.6 拡張機能開発用のウィンドウを起動して 0.5 の一覧を上から実行し、変更前の状態として結果を記録する。ここで既に失敗する項目があれば、それを既知の不具合として記録に残す

## 1. 計測

- [x] 1.1 表示処理の各段階（受け取った文書データの復号、文書の解析、目次の構築、表示内容の画面への反映）に所要時間の測定を入れる
- [x] 1.2 ページごとの処理（描画、文字情報の取得と文字層の構築、縮小画像の生成）に、ページ番号を識別できる形で所要時間の測定を入れる
- [x] 1.3 描画要求ごとに連番を与え、開始、完了、破棄を開発者向けの出力へ記録する
- [ ] 1.4 拡張機能開発用のウィンドウで測定用の文書を開き、各段階の値を `notes/debug/<日付>_render-performance-baseline.md` に記録する。ページ数の少ない文書と数百ページの文書の両方で測る
- [x] 1.5 表示側の自動検証を 1 本追加する。対象はブラウザ機能（DOM とキャンバス）に依存しない部分に限る。文書の解析、文字情報の取得、ページ寸法の取得は Node.js 上で動くので、各ページに異なる語を含む複数ページの文書を読み込み、全ページの語が文字情報から見つかること、見つかった位置のページ番号が正しいこと、各ページの寸法の比が期待どおりであることを確かめる。手順 4 で作る文字索引と枠の高さ計算は、この検証から呼べるモジュールとして切り出す。`npm test` から実行できるようにし、jsdom などの模擬環境は導入しない
- [ ] 1.6 `npm test`、`npm run lint` を実行して成功を確認し、0.5 の手動確認の一覧を実行して変更前と結果が同じであることを確認する

## 2. 解析と描画の処理系の分離

- [ ] 2.1 解析用の処理系の場所を、Webview から読み込める形式の場所として指定する
- [ ] 2.2 文書を開くときの指定から、処理系の分離を無効にする指定を外す
- [ ] 2.3 Webview の HTML を組み立てている箇所から、処理系の本体を画面側へ読み込んでいる記述を外す
- [ ] 2.4 拡張機能開発用のウィンドウで数百ページの文書を開き、読み込み中もスクロールと拡大縮小に応答することを目視で確認する。1.4 と同じ測定を行い、前後の値を記録に並べる
- [ ] 2.5 `npm test`、`npm run lint` を実行して成功を確認し、0.5 の手動確認の一覧を実行して結果を記録する。とくに文字の選択、検索、目次の移動を確認する

## 3. 縮小画像の生成の分離

- [ ] 3.1 縮小画像の生成を本文の描画の流れから切り離し、ページ一覧を表示するときに実行する形にする
- [ ] 3.2 縮小画像の解像度を、一覧での表示寸法に画面の画素密度を掛けた値を目安とする低解像度に変え、本文の拡大率から独立させる
- [ ] 3.3 生成済みの縮小画像をページごとに保持し、一覧を開き直したときと本文の拡大率を変えたときに作り直さないようにする
- [ ] 3.4 一覧を閉じたまま文書を開いたとき、縮小画像の生成の測定値が現れないことを確認し、初回表示時間の前後の値を記録に並べる
- [ ] 3.5 一覧の画質を目視で確認し、`npm test`、`npm run lint` を実行して成功を確認する。0.5 の一覧のうちページ一覧に関する項目を実行して結果を記録する

## 4. 表示中ページと隣接ページだけの描画

- [ ] 4.1 解析した文書を保持して使い回す形にし、拡大率と表示寸法の変更で文書データの復号、解析、目次の構築を繰り返さないようにする。1.4 の測定で拡大率変更時にそれらの段階が現れないことを確認する
- [ ] 4.2 各ページの寸法から正しい高さを持つ空の枠を全ページ分作り、文書を開いた直後から総高さとスクロール範囲が正しいことを確認する
- [ ] 4.3 ページごとに未描画・待ち・描画中・描画済みの状態を持たせ、表示範囲とその隣接ページを描画対象として選ぶ仕組みを入れる
- [ ] 4.4 進行中の描画を中止できるようにし、拡大率と表示寸法の変更時に中止する。中止したことが 1.3 の記録に現れることを確認する
- [ ] 4.5 表示範囲から離れたページの描画結果を解放し、スクロールを続けても保持ページ数が増え続けないことを確認する
- [ ] 4.6 文字の位置情報を描画とは独立に取得して索引として保持し、検索を索引に対して行う形に変える。一致箇所の強調は該当ページを表示したときに行う。1.5 の自動検証が通ることを確認する
- [ ] 4.7 手書きの入力受け付けを、描画結果ではなくページの枠に対して一度だけ登録する形に変える。描画結果の差し替えで登録漏れと二重登録が起きないことを、後から描画されたページへの手書きで確認する
- [ ] 4.8 蛍光ペンと下線と取り消し線、付箋、入力欄、テキスト選択の各処理が、描画していないページを含む状態でも正しく動くようにする。後から描画されたページでそれぞれを操作し、保存して開き直しても内容が保たれることを確認する
- [ ] 4.9 現在ページの判定とページ番号指定の移動、目次からの移動が、描画していないページに対しても正しく動くことを確認する
- [ ] 4.10 合成に関する見た目の指定を、描画結果を持つページだけに付ける形に変える
- [ ] 4.11 `npm test`、`npm run lint` を実行して成功を確認し、0.5 の手動確認の一覧を全項目実行して結果を記録する。初回表示時間と定常時の保持ページ数を、ページ数の少ない文書と数百ページの文書の両方で測り、記録に並べる

## 5. 画素密度に応じた解像度

- [ ] 5.1 描画の解像度の上限を、文書全体の合計から 1 ページあたりの画素数に変え、倍率を原則として画面の画素密度に等しくする
- [ ] 5.2 手書きを描く面の画素数に画面の画素密度を掛け、描画時の座標変換で座標を表示寸法基準のまま保つ
- [ ] 5.3 変更前に保存した手書きを含む文書を開き、筆跡の位置と太さの比が保存時と同じであること、保存し直しても位置がずれないことを確認する
- [ ] 5.4 画素密度が 1 を超える画面で本文と筆跡の見た目を確認し、上限に達するまで拡大しても描画が完了して表示が破綻しないことを確認する
- [ ] 5.5 定常時の保持画素数とメモリ使用量を測り、ページ数の異なる文書の間で同程度であることを確認する。想定を超えていれば 1 ページあたりの上限値を下げ、決めた値と根拠を記録に書く
- [ ] 5.6 `npm test`、`npm run lint` を実行して成功を確認し、0.5 の手動確認の一覧を全項目実行して結果を記録する
- [ ] 5.7 `npm run package` で配布物を作り、`code --install-extension` で導入し直して、日常の使用で問題が出ないことを確認する。変更前後の測定値をまとめて記録に書く
