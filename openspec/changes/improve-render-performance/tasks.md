## 0. 完了済みの土台

- [x] 0.1 導入済み版の指操作に関する修正を作業ツリーへ取り込み、該当ファイルの内容が一致することを確認する
- [x] 0.2 設定上の版を 0.0.8 に上げ、変更履歴へ 0.0.6 から 0.0.8 を追加する
- [x] 0.3 表示処理の各段階と描画要求の開始、完了、破棄を記録できるようにし、既存の自動テストと静的検査が通ることを確認する
- [x] 0.4 複数ページの PDF から文字情報とページ寸法を取得する自動検証を追加し、npm testから実行できることを確認する

## 1. 表示範囲だけの描画

- [x] 1.1 表示対象のページ番号と前後 1 ページを選ぶ処理の自動テストを先に追加し、境界ページ、複数ページ表示、空の入力を検証する
- [x] 1.2 PDF の復号、解析、目次、ページ情報を文書ごとに一度だけ作るセッションへ分け、拡大率変更後も同じ解析結果を使うことを自動検証または診断出力で確認する
- [x] 1.3 全ページ分の正しい寸法を持つ枠を作り、第 1 ページ、表示中のページ、前後 1 ページだけを描画する。末尾へ移動でき、未描画ページが移動後に表示されることを確認する
- [x] 1.4 ページごとの描画要求を保持し、拡大率または表示寸法が変わったときに古い要求を中止する。最後に指定した倍率の内容だけが残ることを確認する
- [x] 1.5 描画が後から完了したページへ手書き、蛍光ペン、下線、取り消し線、付箋、入力欄、テキスト選択、検索結果、現在の操作状態を反映し、保存して開き直しても内容が残ることを確認する
- [x] 1.6 npm testとnpm run lintを実行し、文書の初回表示、拡大縮小、連続スクロール、ページ移動、検索、注釈の保存と再表示を短い動作確認で検証する

## 2. 付随処理の遅延と先読み

- [x] 2.1 PDF.js の処理系の場所を指定して画面側での直接実行を止め、読み込み中も画面操作へ応答することを確認する
- [x] 2.2 ページ一覧を開いたときだけ低解像度の縮小画像を生成して使い回し、本文の拡大率変更では作り直さないことを確認する
- [x] 2.3 文字情報を本文描画から分離して検索索引へ保持し、未描画ページの語を検索して移動と強調ができることを確認する
- [x] 2.4 同時描画数を制限し、スクロール方向のページを優先して先読みする。連続スクロール中に通過済みページの処理が表示先を妨げないことを確認する
- [x] 2.5 npm testとnpm run lintを実行し、文書の表示、検索、ページ一覧、注釈の保存と再表示を短い動作確認で検証する

## 3. 長い文書のスクロール処理

- [x] 3.1 表示範囲から離れたページのキャンバス内容を解放し、同じページへ戻ると再描画されることを確認する
- [x] 3.2 現在ページの判定をスクロールごとの全ページ走査から交差監視へ変更し、ページ番号と目次の選択がスクロールに追従することを確認する
- [x] 3.3 計算後の倍率が現在値と同じ場合は再描画せず、寸法変更後も表示内容と位置が保たれることを確認する
- [x] 3.4 不要になったページの PDF.js 側の資源を解放し、再表示時に文字層、入力欄、注釈が復元されることを確認する
- [x] 3.5 npm testとnpm run lintを実行し、文書の初回表示、拡大縮小、連続スクロール、ページ移動、検索、注釈の保存と再表示を短い動作確認で検証する

## 4. 配布物

- [ ] 4.1 変更履歴を実装済みの内容に揃え、npm run packageで配布物を作れることを確認する
